---
// Pagination is rendered client-side because the result count depends on the
// user's search/filter state. This component only provides the mount point.
---

<nav id="vpp-pagination" class="mt-10 flex flex-wrap items-center justify-center gap-2" aria-label="分页"></nav>
